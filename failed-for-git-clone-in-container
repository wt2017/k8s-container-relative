Because host has set the proxy but container hasn't.
Commands to set proxy:in container as,

export http_proxy=127.0.0.1:7890
export https_proxy=127.0.0.1:7890


For sudo podman build -f Dockerfile, parameter --network=host is required to shared the host network to container image build process.
